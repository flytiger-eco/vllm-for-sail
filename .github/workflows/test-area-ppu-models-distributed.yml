# Models Distributed nightly 测试——对位上游 models_distributed.yaml 的
# Distributed Model Tests(2 GPUs)；PPU 保留 3 个 multi step
# （basic_correctness distributed / sharded_state_loader / transformers
# distributed，均 TP=2 / num_gpus=2），Cmd 4/5/6（language/multimodal/whisper
# distributed）首版跳过。用例选集见 scripts/ppu/test-area-ppu-models-distributed.sh。
#
# nightly 调度由 nightly-ppu.yml 聚合；
# 本文件为单 area 手动入口（workflow_dispatch，用于首跑校准与排障），

# 串行：使用独立 concurrency group（与其余 test-area-ppu-* 一致），不与
#   nightly-ppu.yml 共享 group——共享组会因「1 运行 + 1 等待」的容量限制
#   互相挤掉排队任务。改 K8s 调度后 area 之间可并行（卡由 device plugin
#   真隔离，详见下方 container_options 注释），并行度由 K8s extended
#   resource 容量自然限制：超出节点 PPU 卡数的 Pod 会 Pending 排队。

name: Models Distributed Test (Nightly)

# 不放 schedule/push/PR 触发，避免与聚合 workflow 重复调度。
on:
  workflow_dispatch:
    inputs:
      test_mode:
        description: "TEST_MODE：all(默认) | single | multi"
        type: string
        default: all
      pytest_args:
        description: '追加的 pytest 参数，如 "-k test_foo -x"（透传 PYTEST_EXTRA_ARGS）'
        type: string
        default: ""

permissions:
  contents: read

env:
  PPU_BASE_IMAGE: pkg.flytiger-eco.com/docker_release/llm:v2.1.1-pytorch2.11.0-ubuntu24.04-cuda13.0-vllm0.23.0-py312

jobs:
  # ==================== PPU Models Distributed（nightly 全量 multi 3 段） ==================== #
  ppu-models-distributed-test:
    runs-on: k8s-runner-group-cpu-flytiger
    container:
      image: ghcr.io/actions/actions-runner:latest
    # job 上限 = action timeout_minutes(90) + ~25（K8s 调度+镜像准备）
    timeout-minutes: 115
    concurrency:
      group: test-area-ppu-models-distributed-${{ github.ref }}
      cancel-in-progress: false
    steps:
      # checkout 是 action 源码自动投递的前提：action 把 $GITHUB_WORKSPACE 打包
      # 暂存到 NAS，worker Pod 内解压到 source_dir（默认 /workspace/source）。
      # fetch-depth: 1 缩小 .git，减少打包体积与耗时。
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Build in-pod runner payload
        id: payload
        shell: bash
        env:
          TEST_MODE: ${{ inputs.test_mode || 'all' }}
          PYTEST_EXTRA_ARGS: ${{ inputs.pytest_args || '' }}
        run: |
          # 为什么绕 base64：ppu-distributed-action 的「校验 inputs」步骤把
          # inputs.command 直接文本插值进 `if [ -z "<command>" ]`，多行/含引号的
          # command 会引发 shell syntax error。base64 后是单行且只含
          # [A-Za-z0-9+/=]，被那对双引号整段吞掉，安全无副作用。
          cat > /tmp/ppu-run.sh <<'RUNEOF'
          #!/bin/bash
          set -euo pipefail
          cd /workspace/source
          echo "[k8s-area] node=${NODE_NAME:-unknown} TEST_MODE=${TEST_MODE:-all}"
          bash scripts/ppu/ppu_install_dependency.sh
          bash scripts/ppu/test-area-ppu-models-distributed.sh
          RUNEOF
          # 语法自检：runner 上早失败好过在 Pod 里烧一次调度
          bash -n /tmp/ppu-run.sh
          # tr 而非 base64 -w0：后者是 GNU 专有
          echo "b64=$(base64 < /tmp/ppu-run.sh | tr -d '\n')" >> "$GITHUB_OUTPUT"
          # extra_env 以逗号分隔 KEY=VAL，值内不能含逗号；空值项一律不下发
          envs="HF_HUB_CACHE=/nas_aisw/datasets/hf_cache/hub,PPU_DEVICE_LABEL=OAM-810E"
          envs="${envs},TEST_MODE=${TEST_MODE:-all}"
          if [ -n "${PYTEST_EXTRA_ARGS:-}" ]; then
            envs="${envs},PYTEST_EXTRA_ARGS=${PYTEST_EXTRA_ARGS}"
          fi
          if [ -n "${PPU_WHEEL_PATH:-}" ]; then
            envs="${envs},PPU_WHEEL_PATH=${PPU_WHEEL_PATH}"
          fi
          # /wl_nas（同一 NAS），Publish test summary 步骤按同约定路径读取。
          envs="${envs},PPU_SUMMARY_NAS_DIR=/mnt/wl_nas/devops/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}/${GITHUB_JOB}"
          echo "extra_env=${envs}" >> "$GITHUB_OUTPUT"

      - name: Run PPU models distributed tests
        id: ppu
        uses: flytiger-eco/ppu-distributed-action@main
        with:
          # 单机；nproc_per_node = 本 area 实际需要的 PPU 卡数
          #（3 个 multi step 均 TP=2 / num_gpus=2）
          nnodes: 0
          nproc_per_node: 2
          node_selector: board-type=OAM-810E
          # /nas_aisw、/mnt/wl_nas 由 action 默认挂载，无需传 host_volumes
          image: ${{ env.PPU_BASE_IMAGE }}
          namespace: ppu-sched
          # in-pod 上限（首跑保守值，实测后收紧）
          timeout_minutes: 90
          cleanup_policy: always
          # 刻意不开 privileged：特权容器会拿到宿主机全量 /dev，绕过 PPU device
          # plugin 的设备注入。对照实测（run 34325790779，同节点同样申请 2 卡）：
          # privileged 下 /dev 有 16 个 alixpu_ppu*、ppu-smi 列 16 张、
          # torch device_count=16；unprivileged 下只注入分配到的 2 个设备节点
          # 且重编号为 0..N-1（物理 0/3 → 容器内 0/1）、device_count=2。后者正合
          # 各 area 脚本 CUDA_VISIBLE_DEVICES=0..N-1 的相对语义，且是 area 之间
          # 能并行的前提。host_ipc/shm_size 对位 docker --ipc=host --shm-size=8g；
          # docker 版的 --ulimit memlock=-1 无 K8s 等价物、unprivileged 下亦无法
          # 自行提升，如遇 pinned memory 类失败需集群侧放宽 LimitMEMLOCK。
          container_options: |
            host_ipc: true
            shm_size: "8Gi"
          extra_env: ${{ steps.payload.outputs.extra_env }},PPU_ARTIFACTORY_USER=${{ secrets.PPU_ARTIFACTORY_USER }},PPU_ARTIFACTORY_PASSWORD=${{ secrets.PPU_ARTIFACTORY_PASSWORD }}
          # 必须单行、不含双引号，见上面 payload 步骤注释
          command: echo ${{ steps.payload.outputs.b64 }} | base64 -d > /tmp/ppu-run.sh && bash /tmp/ppu-run.sh

      # 用例明细：脚本在 Pod 内把 per-unit 统计表经 NAS 回传（PPU_SUMMARY_NAS_DIR，
      # 见 Build in-pod runner payload 步骤），此处读回渲染；NAS 读不到时回退为
      # 作业元信息表，明细仍可从 job 日志查看（action 全量透传 Pod stdout）。
      - name: Publish test summary
        if: always()
        env:
          OUT_JOB_NAME: ${{ steps.ppu.outputs.job_name }}
          OUT_JOB_STATUS: ${{ steps.ppu.outputs.job_status }}
          OUT_DURATION: ${{ steps.ppu.outputs.duration_seconds }}
          OUT_TASK_PODS: ${{ steps.ppu.outputs.task_pods }}
          OUT_LOG_ARTIFACT: ${{ steps.ppu.outputs.log_artifact_name }}
          TEST_MODE: ${{ inputs.test_mode || 'all' }}
        run: |
          NAS_SUM="/wl_nas/devops/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}/${GITHUB_JOB}/summary.md"
          {
            if [ -f "${NAS_SUM}" ]; then
              cat "${NAS_SUM}"
              echo ""
              echo "<sub>mode: ${TEST_MODE:-all} · K8s job \`${OUT_JOB_NAME:-<empty>}\` · ${OUT_JOB_STATUS:-<empty>} · ${OUT_DURATION:-<empty>}s · pods: \`${OUT_TASK_PODS:-<empty>}\` · log: \`${OUT_LOG_ARTIFACT:-<empty>}\`</sub>"
            else
              echo "### Models Distributed Test (PPU, K8s)"
              echo ""
              echo "> summary.md 未回传（Pod 内脚本在 EXIT trap 前退出或 NAS 不可写），仅显示作业元信息；用例明细见上方 job 日志。"
              echo ""
              echo "| item | value |"
              echo "|---|---|"
              echo "| test_mode | ${TEST_MODE:-all} |"
              echo "| image | \`${PPU_BASE_IMAGE}\` |"
              echo "| job_name | ${OUT_JOB_NAME:-<empty>} |"
              echo "| job_status | ${OUT_JOB_STATUS:-<empty>} |"
              echo "| duration_seconds | ${OUT_DURATION:-<empty>} |"
              echo "| task_pods | ${OUT_TASK_PODS:-<empty>} |"
              echo "| log_artifact | ${OUT_LOG_ARTIFACT:-<empty>} |"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
